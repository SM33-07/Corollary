# Corollary
Self-hosted PostgreSQL query performance advisor. Scrapes pg_stat_statements, fingerprints queries, and surfaces explainable, evidence-backed recommendations (seq scans, missing indexes, N+1 patterns) — read-only on your database, always.
